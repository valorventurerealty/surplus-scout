<x-layouts.app title="Email Signatures" heading="Email Signatures" subheading="Approved signatures appended at delivery time">
    @include('email._navigation', ['active' => 'signatures'])
    <div class="mx-auto max-w-4xl space-y-4">@foreach($signatures as $signature)<article class="rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900"><div class="flex items-start justify-between gap-4"><div><h2 class="font-bold">{{ $signature->name }}</h2><p class="mt-1 text-xs text-slate-500">{{ $signature->is_default ? 'Default signature' : 'Optional signature' }} · {{ $signature->is_active ? 'Active' : 'Inactive' }}</p></div>@can('update', $signature)<a href="{{ route('email.signatures.edit', $signature) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold dark:border-slate-700">Edit</a>@endcan</div><div class="prose prose-slate mt-5 max-w-none rounded-xl bg-slate-50 p-4 text-sm dark:prose-invert dark:bg-slate-800">{!! $signature->body_html !!}</div></article>@endforeach</div>
</x-layouts.app>
