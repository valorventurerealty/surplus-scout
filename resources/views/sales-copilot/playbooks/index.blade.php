<x-layouts.app title="Sales Copilot Playbooks" heading="Sales Copilot Playbooks" subheading="Approved objection handling and conversation guidance">
@include('sales-copilot._navigation')
<div class="mb-5 flex gap-3"><form method="GET" class="flex flex-1 gap-2"><input name="search" value="{{ request('search') }}" class="form-input mt-0" placeholder="Search playbooks"><button class="rounded-lg border border-slate-300 px-4 dark:border-slate-700">Search</button></form>@if(auth()->user()->canManageArmory())<a href="{{ route('sales-copilot.playbooks.create') }}" class="rounded-lg bg-amber-400 px-4 py-2 text-sm font-bold text-slate-950">+ Add playbook</a>@endif</div>
<div class="grid gap-4 lg:grid-cols-2">@foreach($playbooks as $playbook)<article class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div class="flex items-start justify-between gap-4"><div><h2 class="font-bold">{{ $playbook->title }}</h2><p class="mt-1 text-xs text-slate-500">{{ str($playbook->stage)->replace('_',' ')->title() }} · Priority {{ $playbook->priority }}</p></div><div class="flex gap-1">@if($playbook->owner_authored)<span class="rounded bg-purple-100 px-2 py-1 text-[10px] font-bold text-purple-700">OWNER</span>@endif @if($playbook->vvr_approved)<span class="rounded bg-emerald-100 px-2 py-1 text-[10px] font-bold text-emerald-700">APPROVED</span>@endif</div></div><blockquote class="mt-4 rounded-xl bg-slate-50 p-4 text-sm font-medium dark:bg-slate-800">{{ $playbook->recommended_response }}</blockquote><p class="mt-3 text-sm text-slate-500">{{ $playbook->objective }}</p>@if(auth()->user()->canManageArmory())<a href="{{ route('sales-copilot.playbooks.edit',$playbook) }}" class="mt-4 inline-block text-sm font-semibold text-amber-600">Edit playbook →</a>@endif</article>@endforeach</div><div class="mt-6">{{ $playbooks->links() }}</div>
</x-layouts.app>
