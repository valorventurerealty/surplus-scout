<x-layouts.app title="{{ $playbook->exists ? 'Edit' : 'Add' }} Sales Playbook" heading="{{ $playbook->exists ? 'Edit' : 'Add' }} Sales Playbook" subheading="Governed VVR coaching content">
@include('sales-copilot._navigation')
<form method="POST" action="{{ $playbook->exists ? route('sales-copilot.playbooks.update',$playbook) : route('sales-copilot.playbooks.store') }}" class="mx-auto max-w-5xl space-y-5 rounded-2xl border border-slate-200 bg-white p-6 dark:border-slate-800 dark:bg-slate-900">@csrf @if($playbook->exists)@method('PUT')@endif
<div class="grid gap-4 sm:grid-cols-2"><label class="text-sm font-semibold">Title<input name="title" required value="{{ old('title',$playbook->title) }}" class="form-input mt-2"></label><label class="text-sm font-semibold">Category<input name="category" required value="{{ old('category',$playbook->category ?: 'objections') }}" class="form-input mt-2"></label><label class="text-sm font-semibold">Scenario<input name="scenario" value="{{ old('scenario',$playbook->scenario ?: 'Surplus Recovery') }}" class="form-input mt-2"></label><label class="text-sm font-semibold">Stage<input name="stage" required value="{{ old('stage',$playbook->stage ?: 'objection_resolution') }}" class="form-input mt-2"></label></div>
<label class="block text-sm font-semibold">Trigger phrases — one per line<textarea name="trigger_phrases_text" rows="5" required class="form-input mt-2">{{ old('trigger_phrases_text',implode("\n",$playbook->trigger_phrases ?? [])) }}</textarea></label><label class="block text-sm font-semibold">Recommended response<textarea name="recommended_response" rows="5" required class="form-input mt-2">{{ old('recommended_response',$playbook->recommended_response) }}</textarea></label><label class="block text-sm font-semibold">Objective<textarea name="objective" rows="3" required class="form-input mt-2">{{ old('objective',$playbook->objective) }}</textarea></label>
<div class="grid gap-4 sm:grid-cols-2"><label class="text-sm font-semibold">Tones — comma separated<input name="tones_text" required value="{{ old('tones_text',implode(', ',$playbook->tones ?? ['curious'])) }}" class="form-input mt-2"></label><label class="text-sm font-semibold">Priority<input type="number" name="priority" min="0" max="1000" required value="{{ old('priority',$playbook->priority ?? 50) }}" class="form-input mt-2"></label><label class="text-sm font-semibold">Listen for — one per line<textarea name="listen_for_text" rows="4" class="form-input mt-2">{{ old('listen_for_text',implode("\n",$playbook->listen_for ?? [])) }}</textarea></label><label class="text-sm font-semibold">Likely branches — one per line<textarea name="branches_text" rows="4" class="form-input mt-2">{{ old('branches_text',implode("\n",$playbook->branches ?? [])) }}</textarea></label></div><label class="block text-sm font-semibold">Internal notes<textarea name="notes" rows="3" class="form-input mt-2">{{ old('notes',$playbook->notes) }}</textarea></label><div class="flex flex-wrap gap-5 text-sm">@foreach(['active'=>'Active','vvr_approved'=>'VVR approved','owner_authored'=>'Owner authored'] as $name=>$label)<label class="flex items-center gap-2"><input type="hidden" name="{{ $name }}" value="0"><input type="checkbox" name="{{ $name }}" value="1" @checked(old($name,$playbook->{$name} ?? ($name==='active')))>{{ $label }}</label>@endforeach</div>@if($errors->any())<div class="rounded-xl bg-red-50 p-4 text-sm text-red-700"><ul class="list-disc pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif<div class="flex justify-end"><button class="rounded-lg bg-amber-400 px-5 py-2.5 font-bold text-slate-950">Save playbook</button></div></form>
</x-layouts.app>
