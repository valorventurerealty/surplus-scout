<x-layouts.app title="Website Chat" heading="{{ $conversation->visitor_name }}" subheading="Valorie conversation received {{ $conversation->submitted_at->format('M j, Y \a\t g:i A') }}">
    <div class="mb-5"><a href="{{ route('website-chats.index') }}" class="text-sm font-semibold text-amber-700 hover:underline dark:text-amber-400">← Back to Website Chats</a></div>
    <div class="grid gap-6 lg:grid-cols-[1.3fr_.7fr]">
        <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex flex-wrap items-center justify-between gap-3"><div><p class="text-xs uppercase tracking-wide text-slate-500">{{ $conversation->topic_label }}</p><h2 class="mt-1 text-xl font-semibold">Visitor message</h2></div><span @class(['rounded-full px-3 py-1 text-xs font-semibold','bg-amber-100 text-amber-800'=>$conversation->status==='open','bg-emerald-100 text-emerald-800'=>$conversation->status==='resolved'])>{{ str($conversation->status)->headline() }}</span></div><div class="mt-5 whitespace-pre-wrap rounded-xl bg-slate-50 p-5 text-sm dark:bg-slate-950">{{ $conversation->message }}</div><dl class="mt-6 grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-slate-500">Property address</dt><dd class="mt-1 font-medium">{{ $conversation->property_address ?: 'Not supplied' }}</dd></div><div><dt class="text-slate-500">Parcel ID</dt><dd class="mt-1 font-medium">{{ $conversation->parcel_id ?: 'Not supplied' }}</dd></div><div><dt class="text-slate-500">Source page</dt><dd class="mt-1">@if($conversation->page_url)<a href="{{ $conversation->page_url }}" target="_blank" rel="noopener noreferrer" class="font-medium text-amber-700 hover:underline">Open page ↗</a>@else Not supplied @endif</dd></div><div><dt class="text-slate-500">Consent recorded</dt><dd class="mt-1 font-medium">{{ $conversation->consent_at->format('M j, Y g:i A') }}</dd></div></dl></section>
        <aside class="space-y-6"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Contact</h2><dl class="mt-4 space-y-3 text-sm"><div><dt class="text-slate-500">Name</dt><dd class="font-medium">{{ $conversation->visitor_name }}</dd></div><div><dt class="text-slate-500">Email</dt><dd><a href="mailto:{{ $conversation->visitor_email }}" class="text-amber-700 hover:underline">{{ $conversation->visitor_email }}</a></dd></div><div><dt class="text-slate-500">Phone</dt><dd><a href="tel:{{ $conversation->visitor_phone }}" class="text-amber-700 hover:underline">{{ $conversation->visitor_phone }}</a></dd></div></dl>@if($conversation->contact)<a href="{{ route('contacts.show',$conversation->contact) }}" class="mt-5 inline-flex rounded-lg bg-amber-400 px-4 py-2 text-sm font-semibold text-slate-950">Open CRM contact</a>@endif</section>
        <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Follow-up</h2>@if($conversation->task)<p class="mt-3 text-sm">A high-priority task was created automatically.</p><a href="{{ route('tasks.show',$conversation->task) }}" class="mt-3 inline-block text-sm font-semibold text-amber-700 hover:underline">Open task →</a>@endif @can('update',$conversation)<form method="POST" action="{{ route('website-chats.update',$conversation) }}" class="mt-5">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $conversation->status === 'open' ? 'resolved' : 'open' }}"><button class="w-full rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold dark:border-slate-700">Mark {{ $conversation->status === 'open' ? 'resolved' : 'open' }}</button></form>@endcan</section></aside>
    </div>
</x-layouts.app>
