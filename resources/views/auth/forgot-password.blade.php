<x-layouts.guest title="Reset password"><h1 class="text-2xl font-bold">Reset your password</h1><p class="mt-2 text-sm text-slate-400">Enter your work email and we will send a secure reset link.</p>@if(session('status'))<div class="mt-5 rounded-lg bg-emerald-950 px-4 py-3 text-sm text-emerald-300">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}" class="mt-6 space-y-5">@csrf<div><label for="email" class="text-sm font-medium">Email address</label><input id="email" name="email" type="email" value="{{ old('email') }}" required autofocus class="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 outline-none ring-amber-400 focus:ring-2"><x-form-error name="email" /></div><button class="w-full rounded-lg bg-amber-400 px-4 py-3 font-semibold text-slate-950">Send reset link</button><a href="{{ route('login') }}" class="block text-center text-sm text-slate-400">Return to sign in</a></form></x-layouts.guest>
