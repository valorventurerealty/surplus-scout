<x-layouts.app title="Deals" heading="Deals" subheading="Acquisition, disposition, surplus, PreTax Auction, and rental transactions">
    <div class="mb-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Open',$metrics['open']],['Under contract',$metrics['under_contract']],['Closing',$metrics['closing']],['Closed',$metrics['closed']]] as [$label,$value])<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p class="text-xs font-semibold uppercase tracking-wide text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-bold">{{ number_format($value) }}</p></div>@endforeach</div>
    <div class="mb-6 flex flex-col justify-between gap-4 xl:flex-row"><form method="GET" class="grid flex-1 gap-3 sm:grid-cols-2 xl:grid-cols-[minmax(220px,1fr)_180px_170px_180px_auto]">@if(request('sort'))<input type="hidden" name="sort" value="{{ request('sort') }}"><input type="hidden" name="direction" value="{{ request('direction', 'asc') }}">@endif<input name="search" value="{{ request('search') }}" placeholder="Search deal number or title" class="form-input mt-0"><select name="type" class="form-input mt-0"><option value="">All classifications</option>@foreach($dealTypes as $type)<option value="{{ $type->value }}" @selected(request('type')===$type->value)>{{ $type->label() }}</option>@endforeach</select><select name="status" class="form-input mt-0"><option value="">All statuses</option>@foreach($dealStatuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><select name="assigned_user_id" class="form-input mt-0"><option value="">All assigned users</option>@foreach($assignees as $user)<option value="{{ $user->id }}" @selected((string)request('assigned_user_id')===(string)$user->id)>{{ $user->name }}</option>@endforeach</select><button class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold dark:border-slate-700">Filter</button></form>@can('create',\App\Models\Deal::class)<a href="{{ route('deals.create') }}" class="rounded-lg bg-amber-400 px-4 py-2 text-center text-sm font-semibold text-slate-950">+ Add deal</a>@endcan</div>
    <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="w-full min-w-[1050px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wide text-slate-500 dark:bg-slate-800/60"><tr><x-sortable-header route="deals.index" column="deal" label="Deal" /><x-sortable-header route="deals.index" column="property" label="Property" /><x-sortable-header route="deals.index" column="primary_contact" label="Primary contact" /><x-sortable-header route="deals.index" column="assigned" label="Assigned" /><x-sortable-header route="deals.index" column="close_date" label="Close date" />@if(auth()->user()->canViewPropertyFinancials())<x-sortable-header route="deals.index" column="contract_projected" label="Contract / projected" />@endif<x-sortable-header route="deals.index" column="status" label="Status" /></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($deals as $deal)<tr><td class="px-5 py-4"><a href="{{ route('deals.show',$deal) }}" class="font-semibold hover:text-amber-600">{{ $deal->title }}</a><p class="mt-1 text-xs text-slate-500">{{ $deal->deal_number }} · {{ $deal->type->label() }}</p></td><td class="px-5 py-4">{{ $deal->property?->address ?: 'Unlinked' }}</td><td class="px-5 py-4">{{ $deal->primaryContact?->full_name ?: 'Unassigned' }}</td><td class="px-5 py-4">{{ $deal->assignedUser?->name ?: 'Unassigned' }}</td><td class="px-5 py-4">{{ $deal->projected_close_date?->format('M j, Y') ?: '—' }}</td>@can('viewFinancials',$deal)<td class="px-5 py-4">{{ $deal->contract_amount !== null ? '$'.number_format((float)$deal->contract_amount,2) : '—' }}<p class="mt-1 text-xs text-slate-500">Revenue: {{ $deal->projected_revenue !== null ? '$'.number_format((float)$deal->projected_revenue,2) : '—' }}</p></td>@endcan<td class="px-5 py-4"><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold dark:bg-slate-800">{{ $deal->status->label() }}</span></td></tr>@empty<tr><td colspan="{{ auth()->user()->canViewPropertyFinancials()?7:6 }}" class="px-6 py-14 text-center text-slate-500">No deals match the current filters.</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 px-5 py-4 dark:border-slate-800">{{ $deals->links() }}</div></section>
</x-layouts.app>
