<x-layouts.app title="Start {{ $script->title }}" heading="Start Guided Session" subheading="{{ $script->title }}">
    <form method="POST" action="{{ route('armory.sessions.store', $script, false) }}" class="mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900">@csrf
        <h2 class="text-lg font-bold">Session context</h2><p class="mt-1 text-sm text-slate-500">Context fills approved variables in the script. It does not change CRM records.</p>
        <div class="mt-6 grid gap-5 sm:grid-cols-2"><div class="sm:col-span-2"><label class="text-sm font-medium">Contact</label><select name="contact_id" class="form-input"><option value="">No contact selected</option>@foreach($contacts as $contact)<option value="{{ $contact->id }}" @selected(old('contact_id') == $contact->id)>{{ $contact->full_name }}{{ $contact->company ? ' · '.$contact->company : '' }}</option>@endforeach</select><x-form-error name="contact_id" /></div><div class="sm:col-span-2"><label class="text-sm font-medium">Property</label><select name="property_id" class="form-input"><option value="">No property selected</option>@foreach($properties as $property)<option value="{{ $property->id }}" @selected(old('property_id') == $property->id)>{{ $property->address }} · {{ $property->city }}, {{ $property->state }}</option>@endforeach</select><x-form-error name="property_id" /></div><div class="sm:col-span-2"><label class="text-sm font-medium">Caller name override</label><input name="caller_name" maxlength="180" value="{{ old('caller_name') }}" placeholder="Optional name if the caller is not the selected contact" class="form-input"><x-form-error name="caller_name" /></div></div>
        <div class="mt-6 flex justify-end gap-3"><a href="{{ route('armory.show',$script) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-medium dark:border-slate-700">Cancel</a><button type="submit" class="rounded-lg bg-amber-400 px-5 py-2 text-sm font-semibold text-slate-950">Start session</button></div>
    </form>
</x-layouts.app>
