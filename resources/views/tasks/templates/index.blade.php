<x-layouts.app title="Task Templates" heading="Task templates" subheading="Reusable standards for recurring VVR work">
    <div class="mb-6 flex justify-between gap-3"><a href="{{ route('tasks.index') }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-medium dark:border-slate-700">← Tasks</a>@can('create', \App\Models\TaskTemplate::class)<a href="{{ route('task-templates.create') }}" class="rounded-lg bg-amber-400 px-4 py-2 text-sm font-semibold text-slate-950">+ Add template</a>@endcan</div>
    <div class="grid gap-4 lg:grid-cols-2">@forelse($templates as $template)<section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex items-start justify-between gap-4"><div><div class="flex flex-wrap items-center gap-2"><h2 class="font-semibold">{{ $template->name }}</h2>@unless($template->is_active)<span class="rounded-full bg-slate-200 px-2 py-0.5 text-[10px] font-semibold dark:bg-slate-800">Inactive</span>@endunless</div><p class="mt-2 text-sm">{{ $template->title }}</p><p class="mt-2 line-clamp-2 text-xs leading-5 text-slate-500">{{ $template->description ?: 'No default description.' }}</p></div><span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs dark:bg-slate-800">{{ $template->priority->label() }}</span></div><div class="mt-4 flex flex-wrap gap-3 text-xs text-slate-500"><span>Due in {{ $template->due_in_days ?? '—' }} days</span>@if($template->recurrence_frequency)<span>{{ $template->recurrence_frequency->label() }}</span>@endif</div><div class="mt-5 flex gap-3">@if($template->is_active)<a href="{{ route('tasks.create', ['template' => $template]) }}" class="text-sm font-semibold text-amber-700 hover:underline dark:text-amber-400">Use template</a>@endif @can('update', $template)<a href="{{ route('task-templates.edit', $template) }}" class="text-sm font-semibold hover:underline">Edit</a>@endcan @if($template->is_active) @can('delete', $template)<form method="POST" action="{{ route('task-templates.destroy', $template) }}" onsubmit="return confirm('Deactivate this template?')">@csrf @method('DELETE')<button class="text-sm font-semibold text-rose-700 hover:underline dark:text-rose-400">Deactivate</button></form>@endcan @endif</div></section>@empty<p class="rounded-2xl border border-dashed border-slate-300 p-12 text-center text-sm text-slate-500 lg:col-span-2 dark:border-slate-700">No task templates are configured.</p>@endforelse</div><div class="mt-6">{{ $templates->links() }}</div>
</x-layouts.app>
