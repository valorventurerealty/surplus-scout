<x-layouts.app title="{{ $case->case_number }}" heading="{{ $case->case_number }}" subheading="PreTax Auction acquisition">
    <div class="mx-auto max-w-6xl">
        <div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div class="flex flex-wrap gap-2"><span class="rounded-full bg-amber-100 px-3 py-1 text-xs font-semibold text-amber-800 dark:bg-amber-950 dark:text-amber-300">{{ $case->status->label() }}</span><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold dark:bg-slate-800">Entitlement: {{ $case->entitlement_status->label() }}</span>@if($case->auction_at?->isPast() && $case->status->isOpen())<span class="rounded-full bg-rose-100 px-3 py-1 text-xs font-semibold text-rose-700 dark:bg-rose-950 dark:text-rose-300">Auction date passed</span>@endif</div><div class="flex gap-2">@can('update',$case)<a href="{{ route('pre-auction.edit',$case) }}" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-medium dark:border-slate-700">Edit acquisition</a>@endcan @can('delete',$case)<form method="POST" action="{{ route('pre-auction.destroy',$case) }}" onsubmit="return confirm('Archive this PreTax Auction acquisition?')">@csrf @method('DELETE')<button class="rounded-lg border border-rose-300 px-4 py-2 text-sm font-medium text-rose-700 dark:border-rose-900">Archive</button></form>@endcan</div></div>
        <div class="grid gap-6 lg:grid-cols-[minmax(0,2fr)_minmax(300px,1fr)]"><div class="space-y-6">
            <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Acquisition overview</h2><dl class="mt-5 grid gap-5 sm:grid-cols-2">
                <div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Current owner</dt><dd class="mt-1 text-sm">@if($case->ownerContact)<a href="{{ route('contacts.show',$case->ownerContact) }}" class="font-semibold text-amber-700 hover:underline dark:text-amber-400">{{ $case->ownerContact->full_name }}</a><span class="block text-xs text-slate-500">{{ $case->ownerContact->company ?: $case->ownerContact->email }}</span>@else — @endif</dd></div>
                <div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">Property</dt><dd class="mt-1 text-sm">@if($case->property)<a href="{{ route('properties.show',$case->property) }}" class="font-semibold text-amber-700 hover:underline dark:text-amber-400">{{ $case->property->address }}</a><span class="block text-xs text-slate-500">{{ $case->property->city }}, {{ $case->property->state }} {{ $case->property->postal_code }}</span>@else — @endif</dd></div>
                @foreach([['Assigned to',$case->assignedUser?->name],['Lead source',$case->source],['County',$case->county.' County'],['Parcel ID',$case->parcel_id],['Tax deed number',$case->tax_deed_number],['Certificate number',$case->certificate_number],['Assessor market value',$case->assessor_market_value !== null ? '$'.number_format((float) $case->assessor_market_value,2) : null],['Recording instrument',$case->recording_instrument_number]] as [$label,$value])<div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">{{ $label }}</dt><dd class="mt-1 text-sm">{{ $value ?: '—' }}</dd></div>@endforeach
            </dl></section>
            @if($case->appraiser_url || $case->property_details_url)<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Research sources</h2><div class="mt-4 flex flex-wrap gap-3">@if($case->appraiser_url)<a href="{{ $case->appraiser_url }}" target="_blank" rel="noopener noreferrer" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold dark:border-slate-700">Open property appraiser ↗</a>@endif @if($case->property_details_url)<a href="{{ $case->property_details_url }}" target="_blank" rel="noopener noreferrer" class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold dark:border-slate-700">Open property details ↗</a>@endif</div></section>@endif
            <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Critical timeline</h2><dl class="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">@foreach([['Purchase deadline',$case->purchase_deadline],['Contract date',$case->contract_date],['Closing date',$case->closing_date],['Deed recorded',$case->deed_recorded_date],['Auction',$case->auction_at],['Claim submitted',$case->claim_submitted_at],['Paid',$case->paid_at]] as [$label,$date])<div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">{{ $label }}</dt><dd class="mt-1 text-sm font-medium">{{ $date?->format($label === 'Auction' ? 'M j, Y g:i A' : 'M j, Y') ?: '—' }}</dd></div>@endforeach</dl>@if($case->auction_url)<a href="{{ $case->auction_url }}" target="_blank" rel="noopener noreferrer" class="mt-5 inline-flex text-sm font-semibold text-amber-700 hover:underline dark:text-amber-400">Open auction page ↗</a>@endif</section>
            @can('viewFinancials',$case)<section class="rounded-2xl border border-amber-200 bg-amber-50/60 p-6 shadow-sm dark:border-amber-900 dark:bg-amber-950/20"><h2 class="font-semibold">Acquisition economics</h2><dl class="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">@foreach([['Purchase price',$case->purchase_price],['Closing costs',$case->closing_costs],['Other costs',$case->other_costs],['Total acquisition cost',$case->total_acquisition_cost],['Projected surplus',$case->projected_surplus],['Projected net',$case->projected_net],['Surplus generated',$case->surplus_generated],['Amount recovered',$case->amount_recovered],['Actual net',$case->actual_net]] as [$label,$amount])<div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-400">{{ $label }}</dt><dd class="mt-1 text-lg font-semibold">{{ $amount !== null ? (((float)$amount < 0 ? '−$' : '$').number_format(abs((float)$amount),2)) : '—' }}</dd></div>@endforeach</dl></section>@endcan
            <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Entitlement review</h2><div class="mt-4 rounded-xl bg-slate-50 p-4 dark:bg-slate-950"><p class="font-semibold">{{ $case->entitlement_status->label() }}</p><p class="mt-1 text-xs text-slate-500">Reviewed {{ $case->entitlement_reviewed_at?->format('M j, Y') ?: 'not yet' }}{{ $case->entitlementReviewer ? ' by '.$case->entitlementReviewer->name : '' }}</p><p class="mt-3 whitespace-pre-line text-sm leading-6">{{ $case->entitlement_notes ?: 'No legal or title review basis has been recorded.' }}</p></div><p class="mt-4 text-xs leading-5 text-slate-500">The application records human review. It does not determine legal entitlement or submit a claim.</p></section>
            <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Associated people</h2><div class="mt-4 divide-y divide-slate-100 dark:divide-slate-800">@forelse($case->contacts as $contact)<div class="flex justify-between gap-4 py-3"><div><a href="{{ route('contacts.show',$contact) }}" class="font-semibold text-amber-700 hover:underline dark:text-amber-400">{{ $contact->full_name }}</a><p class="mt-1 text-xs text-slate-500">{{ \App\Enums\PreAuctionContactRole::tryFrom($contact->pivot->role)?->label() }}{{ $contact->pivot->relationship_notes ? ' · '.$contact->pivot->relationship_notes : '' }}</p></div>@can('update',$case)@if($contact->id !== $case->owner_contact_id)<form method="POST" action="{{ route('pre-auction.contacts.destroy',[$case,$contact->pivot->id]) }}">@csrf @method('DELETE')<button class="text-xs font-semibold text-rose-600">Remove</button></form>@endif@endcan</div>@empty<p class="py-3 text-sm text-slate-500">No people linked.</p>@endforelse</div>@can('update',$case)<form method="POST" action="{{ route('pre-auction.contacts.store',$case) }}" class="mt-5 grid gap-3 rounded-xl bg-slate-50 p-4 dark:bg-slate-950 sm:grid-cols-2">@csrf<div class="sm:col-span-2"><select name="contact_id" required class="form-input"><option value="">Select an existing contact</option>@foreach($availableContacts as $contact)<option value="{{ $contact->id }}">{{ $contact->full_name }}{{ $contact->company ? ' · '.$contact->company : '' }}</option>@endforeach</select></div><select name="role" required class="form-input">@foreach($contactRoles as $role)<option value="{{ $role->value }}">{{ $role->label() }}</option>@endforeach</select><input name="relationship_notes" maxlength="500" placeholder="Relationship notes" class="form-input"><div class="sm:col-span-2"><button class="rounded-lg bg-amber-400 px-4 py-2 text-sm font-semibold text-slate-950">Link contact</button></div></form>@endcan</section>
            <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Notes</h2><p class="mt-4 whitespace-pre-line text-sm leading-6 text-slate-600 dark:text-slate-300">{{ $case->notes ?: 'No notes have been added.' }}</p></section>
        </div><aside class="space-y-6">
            <section class="rounded-2xl border border-amber-200 bg-amber-50 p-5 dark:border-amber-900 dark:bg-amber-950/25"><h2 class="font-semibold">Strategy controls</h2><dl class="mt-4 space-y-4 text-sm"><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Non-redemption reviewed</dt><dd class="mt-1 font-medium">{{ $case->non_redemption_reviewed_at?->format('M j, Y') ?: 'Not reviewed' }}</dd><dd class="text-xs text-slate-500">{{ $case->nonRedemptionReviewer?->name }}</dd></div><div><dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Deed before auction</dt><dd class="mt-1 font-medium">{{ $case->deed_recorded_date && $case->auction_at && $case->deed_recorded_date->lt($case->auction_at) ? 'Recorded before auction' : 'Not confirmed' }}</dd></div></dl></section>
            @can('viewDocuments',$case)<section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 class="font-semibold">Private documents</h2>@if($case->document_drive_url)<a href="{{ $case->document_drive_url }}" target="_blank" rel="noopener noreferrer" class="mt-4 inline-flex text-sm font-semibold text-amber-700 hover:underline dark:text-amber-400">Open Drive folder ↗</a>@else<p class="mt-3 text-sm text-slate-500">No Drive folder linked.</p>@endif</section>@endcan
            <section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="flex items-center justify-between"><h2 class="font-semibold">Tasks</h2>@can('create',\App\Models\Task::class)<a href="{{ route('tasks.create',['subject'=>'pre_auction:'.$case->id]) }}" class="text-xs font-semibold text-amber-700">Add task</a>@endcan</div><div class="mt-3 divide-y divide-slate-100 dark:divide-slate-800">@forelse($case->tasks as $task)<a href="{{ route('tasks.show',$task) }}" class="block py-3"><p class="text-sm font-medium">{{ $task->title }}</p><p class="mt-1 text-xs text-slate-500">{{ $task->status->label() }} · {{ $task->due_at?->format('M j, Y') ?: 'No due date' }}</p></a>@empty<p class="py-3 text-sm text-slate-500">No linked tasks.</p>@endforelse</div></section>
        </aside></div><a href="{{ route('pre-auction.index') }}" class="mt-6 inline-flex text-sm font-semibold text-amber-700">← Back to PreTax Auctions</a>
    </div>
</x-layouts.app>
