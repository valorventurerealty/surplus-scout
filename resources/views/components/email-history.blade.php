<section class="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900">
    <div class="flex items-center justify-between gap-3"><div><h2 class="font-semibold">Email history</h2><p class="mt-1 text-xs text-slate-500">Outbound messages linked directly to this record.</p></div><a href="{{ route('email.index') }}" class="text-xs font-semibold text-amber-700 hover:underline dark:text-amber-400">Open Email</a></div>
    <div class="mt-4 divide-y divide-slate-100 dark:divide-slate-800">@forelse($emails as $email)<a href="{{ route('email.show',$email) }}" class="flex flex-col justify-between gap-2 py-3 hover:text-amber-700 sm:flex-row sm:items-center"><span><span class="block text-sm font-medium">{{ $email->subject }}</span><span class="mt-1 block text-xs text-slate-500">To {{ implode(', ', $email->to_json) }} · {{ $email->user->name }}</span></span><span class="text-xs font-semibold">{{ $email->status->label() }} · {{ ($email->sent_at ?? $email->created_at)->format('M j, Y') }}</span></a>@empty<p class="py-5 text-sm text-slate-500">No outbound email is linked to this record.</p>@endforelse</div>
</section>
