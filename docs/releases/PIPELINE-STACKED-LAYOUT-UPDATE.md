# Pipeline stacked layout

The Property Pipeline now renders each status as a full-width vertical section in operating order. Properties inside a status use a responsive card grid:

- one card per row on small screens
- two cards per row on medium screens
- three or four cards per row on larger screens

The horizontal Kanban track and its left/right page scrolling have been removed. Stage movement, filters, permissions, counts, and financial visibility are unchanged.
